# 《你是什么东西》（whatami）部署上线指南

> 执行端文档 · 2026-08-05 · 按步骤操作
> 品牌英文名：**whatami**（仓库名、域名推荐用此）
> 部署完成后：分享长图里的二维码自动生效（无需改代码，二维码内容 = 线上 URL）

## 一、先选路径

| | 路径 A：国内正式上线（推荐） | 路径 B：海外免费托管（快速） |
| --- | --- | --- |
| 费用 | 域名约 60 元/年 + 托管约 100-300 元/年 | 域名约 60 元/年（可先用免费子域名） |
| ICP 备案 | **需要**（1-2 周） | 不需要 |
| 微信内打开/分享 | 正常 | 可能提示"已停止访问"，需复制链接到浏览器 |
| 国内访问速度 | 快 | 一般 |
| 适合场景 | 正式运营、裂变传播 | 先让朋友在浏览器里体验 |

**建议：白天先按路径 B 上线**（半天搞定，浏览器里先跑通、发朋友验证），**同时启动路径 A 的备案**（备案周期长，越早开始越好），备案通过后再切到 A。

---

## 二、路径 A：国内正式上线（微信可分享）

### 第 1 步：买域名（约 10 分钟）

- 阿里云（万网）或腾讯云，搜索域名购买。`.com` 约 55-90 元/年
- 命名建议：`whatami.com`（大概率已被占用）、`whatami.life` / `whatami.today` / `getwhatami.com` 等变体均可考虑
- 买完域名即实名认证（身份证正反面，当天过）

### 第 2 步：买国内云托管（备案的前提）

备案要求服务器/托管在**国内云厂商**，两个选择：

**方案 1：对象存储 COS / OSS（推荐，最便宜）**
- 腾讯云控制台 → 对象存储 COS → 创建存储桶（地域选"成都/广州"等）
- 存储桶开启"静态网站托管"，访问权限设"公有读"
- 费用：几元/月（流量小的话）

**方案 2：轻量应用服务器（需要自己配 Nginx）**
- 腾讯云轻量应用服务器（2C2G 约 100-300 元/年）→ 宝塔面板装 Nginx → 把项目放 `/www/wwwroot/` 下
- 灵活但维护成本高，个人小项目用方案 1 足够

### 第 3 步：ICP 备案（周期 1-2 周，尽早启动）

- 在买托管的云厂商控制台 → "备案"入口开始
- 需要准备：身份证正反面照片、人脸核验（云厂商小程序/APP）、手机号接收短信
- 流程：填写主体信息 → 云厂商初审 → 管局审核 → 通过后下发备案号
- **备案期间**：域名不能绑定国内服务器访问（可用 `IP:端口` 或海外临时托管先体验）

### 第 4 步：上传项目文件（10 分钟）

用 COS 控制台的"上传文件"或 COSBrowser 客户端，把以下传到存储桶根目录：

```
index.html
css/style.css
js/data.js
js/app.js
assets/animals/  （16 个 SVG）
build/index.html （单文件版，备用）
```

### 第 5 步：域名解析 + HTTPS

- **解析**：DNSPod（域名管理）→ 添加记录：
  - `@` 记录 → CNAME → COS 提供的静态网站域名（COS 控制台能看到）
  - `www` 记录 → 同上
- **HTTPS 证书**：腾讯云"SSL 证书"控制台 → 申请**免费 DV 证书**（一年有效）→ 绑定你的域名 → 在 COS 控制台"自定义域名 + HTTPS"里配置证书
- COS 控制台 → 域名管理 → 添加自定义域名（填你的域名），开启强制 HTTPS

### 第 6 步：上线验证

- 手机浏览器访问 `https://你的域名` → 完整跑一遍答题流程
- 手机微信里打开 → 确认不拦截、能正常打开
- 微信右上角分享 → 检查分享卡片（标题/描述/缩略图）
- 生成分享长图 → 扫长图二维码 → 应能打开测试页（**二维码自动生效**）

---

## 三、路径 B：海外免费托管（先跑通，半天）

### GitHub Pages（免费，无需信用卡）

1. 注册 GitHub（github.com）→ 新建仓库（Public）
2. 上传项目文件（index.html + css/ + js/ + assets/）
3. 仓库 Settings → Pages → Source 选 main 分支 → 保存
4. 等待 1-2 分钟 → 得到地址 `https://你的用户名.github.io/仓库名/`
5. 手机浏览器直接访问该地址即可

### Vercel（免费，部署更快）

1. 注册 vercel.com（用 GitHub 账号登录）
2. New Project → Import 仓库 → Deploy（自动识别静态站）
3. 得到 `https://xxx.vercel.app` 地址

> 注意：这两个域名微信内打开可能提示"已停止访问"，用户需复制链接到浏览器——用于先体验和验证功能，正式传播走路径 A。

---

## 四、上线后检查清单

- [ ] `https://你的域名` 浏览器可访问，页面正常（深色封面）
- [ ] 完整答题 30 题 → 结果页正常
- [ ] 生成分享长图 → 长图二维码可被扫描 → 打开测试页
- [ ] 微信内打开不拦截（路径 A）
- [ ] 微信分享卡片有标题/描述/缩略图（见下）
- [ ] 手机各机型字体/布局正常

### 微信分享缩略图（需准备一张图）

- 微信分享卡片取页面 `og:image` 指向的图片（当前页面只有 og:title/description，没有 og:image）
- 建议：准备一张 **500×400 左右 PNG** 封面缩略图（可以用结果页截图或设计一张"你是什么东西"文字图），上传到项目后，在 index.html 的 `<head>` 加一行：
  ```html
  <meta property="og:image" content="https://你的域名/thumb.png">
  ```
- 搞定后告诉我，我来加这一行

---

## 五、费用与时间预估

| 项目 | 费用 | 时间 |
| --- | --- | --- |
| 域名 | 约 55-90 元/年 | 10 分钟 |
| 备案 | 免费 | 7-15 天（等待为主） |
| COS 托管 + CDN | 约 1-10 元/月 | 1 小时 |
| SSL 证书 | 免费（腾讯云 DV） | 申请后当天生效 |
| GitHub Pages / Vercel | 免费 | 30 分钟 |

**合计**：正式上线首年约 60-180 元；快速上线 0 元（用免费子域名）。

---

## 六、常见坑

1. **备案前不要解析域名到国内服务器**——会被检查拦截
2. COS 的 CNAME 解析生效需要几分钟到几小时，别急
3. 证书申请后要"部署"到 COS/服务器才算生效（不是申请完就行）
4. 手机测试用**无痕模式/换流量**，避免旧缓存
5. 微信内打开异常时，先确认域名已备案（未备案域名在微信内会被提示）
