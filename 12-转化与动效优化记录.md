# 12 · 转化与动效优化记录

> 轮次：2026-08 中 · 依据 skill：scorecard-marketing / top-design / microinteractions / web-typography / steve-jobs-design-review
> 目标：补上漏斗最后一个漏洞（中途流失回收）+ 把"没人截图"的 UI 提升到"签名时刻"级。

## 一、P0 转化漏斗（scorecard-marketing：9/10 → 10/10）

原诊断：30 题偏长，答到第 18 题关掉的人直接蒸发，无任何回收机制。两个修复：

### 1. 断点续答

- 每答一题把 `{mode, idx, scores}` 写入 `localStorage[wa_progress]`
- 回访时封面出现红色「继续上次测试（第 N / 30 题）」按钮，点击从断点继续，分数已恢复
- 答完整套自动清除进度；点"开始测试"会重新开始并清除旧进度

### 2. 快测模式 + 补测升级

| 模式 | 题目 | 说明 |
| --- | --- | --- |
| `full` | 30 题 | 原完整版，不变 |
| `quick` | 12 题 | 每维抽第 1/4/7 题（3 题为奇数 → 无平局），封面「没耐心？12 题快测版 →」入口 |
| `upgrade` | 18 题 | 快测结果页红色「再答 18 题，升级完整档案」→ 补剩余 18 题（含 2 道彩蛋题），**快测分保留累加**，最终仍是每维 7 题完整计分 |

- 快测结果页：显示「快测版 · 结果仅供参考」注记，稀有度徽章隐藏（极端度未满，显示会失真），升级后徽章出现
- 快测答完关掉页面 → 回访时封面显示「继续升级完整档案（还差 18 题）」
- 新增统计事件：`开始快测` / `快测完成` / `升级补测` / `升级完成` / `恢复进度`

设计意图：把 30 题拆成「12 题漏斗 + 18 题漏斗」，先给即时反馈降低流失，再用已投入的 12 题做沉没成本驱动升级。

## 二、P1 视觉签名（top-design：3/11 → 约 8/11）

### 1. 标题字体：思源宋体 Heavy 本地子集

- 「档案/公文」感宋体，仅用于三个签名标题：封面主标题、结果页人格名、画廊人格名
- 从思源宋体 Heavy（SIL OFL，可免费商用）用 pyftsubset 子集化：**24MB OTF → 17KB WOFF2**（60 个用字 + 常用标点数字），零 CDN，微信可加载
- `font-display: swap` + 系统宋体 fallback（Noto Serif SC / SimSun）
- 重新生成：`python build/gen_font_subset.py`；单文件构建时自动 base64 内联（`build_single.py`）

### 2. 盖章动画（全站签名时刻）

- 红印章「非正式」以 `cubic-bezier(.16,1,.3,1)` 从 `scale(2.6)` → 1「啪」地盖下（约 0.5s）
- 封面：进页面 0.4s 后盖一次；结果页：**每次**渲染结果（含画廊查看他人档案）重触发
- 与「地狱判决」内容互文——这就是全站最值得截图的一帧；长图截图前等待 `document.fonts.ready`，保证截到宋体而非 fallback

## 三、P2 动效系统（microinteractions）

| 项 | 改动 |
| --- | --- |
| 缓动曲线 | 全局 `--ease-out: cubic-bezier(.16,1,.3,1)`（出场）、`--ease-back: cubic-bezier(.34,1.56,.64,1)`（弹性），替换所有裸 `ease` |
| 选项入场 | slide-up stagger：每题两选项 120ms / 210ms 错开入场 |
| 题目切换 | 题干 fadeUp 重触发动画 |
| 进度条 | 弹性填充（back-out 曲线），答完一题"弹"到新位置 |
| 按钮 pressed | `.btn-main / .btn-ghost / .option` 按压 `scale(.97)`，150ms 反馈 |
| 结果卡片 | 整卡 fadeUp 入场；稀有度徽章 `badgePop` 弹性弹出 |
| 加载页 | 文案 softPulse 呼吸动画（已有 5 条随机文案） |
| 微细节 | `::selection` 品牌色、`:focus-visible` 描边（键盘可达性） |

## 四、Steve Jobs 设计审查

```
# Design Review: 《你是什么东西》转化 + 动效轮
**Verdict:** INSANELY GREAT（8/10 → 本轮修复后 10/10）
**The One Thing:** 30 题答完出地狱档案，且答不完的人也能被拉回来。
**Keeps its promise?** yes — 自动化验收 20/20：断点续答、快测、升级、盖章、字体全部生效
**Cut list:**
  - 快测结果页不做稀有度徽章（极端度失真，砍掉换诚实）
  - 不做"补 18 题"以外的任何升级激励（不加倒数/紧迫感，靠已投入驱动）
**Fix list（本轮已修）:**
  1. 中途流失回收：断点续答 + 快测/补测双漏斗（最高杠杆）
  2. 标题字体本地化（签名时刻的第一眼）
  3. 盖章动画（全站最值得截图的一帧）
  4. 全站缓动曲线 + 微交互（30 题 × 2 秒/题，动效撑节奏）
**Back of the fence（未完成面，下轮再做）:**
  - 404 页 / 离线态：静态站无 404 处理，GitHub Pages 默认页可用但未定制
  - 长图失败态：html2canvas 失败时仅有 alert，可加更友好的重试提示
  - 200% 缩放下标题换行检查（真机待验）
```

## 五、验证

- Playwright 自动化验收 `build/test_verify.py`（20 项断言全过）：快测入口、字体加载、断点续答恢复、快测 12 题、升级按钮/注记、回访"继续升级"、补测 18 题、稀有度出现、盖章类、console 零错误
- DOM 计算样式确认：封面标题 `font-family` 生效 WADisplay、印章动画结束回落 `rotate(-15deg)`
- `node --check` 语法通过；源码 / deploy 四文件 MD5 一致；`build/index.html` 重建（347KB，字体 base64 内联）

## 六、遗留

- 微信卡片缓存：本轮改了封面与标题，分享时带 `?v=6` 可强制刷新卡片
- 上线后盯三个新比率：**快测入口点击率**（封面曝光转化）、**快测→升级率**（12 题漏斗收口）、**恢复进度率**（流失回收有效性）

## 七、分享长图 → 分享卡片（居中卡片式改版）

**需求**：生成图不要"整条测试结果"长截图，改为一张居中、类似卡片的分享图。

**实现**
- 新增 `#share-card`（屏幕外渲染的截图源）：750×1000（3:4），白底画布 28px 留白 + 居中圆角档案卡（694×944）
- 卡片结构：深蓝紫 hero（品牌行 + 红印章 + 动物大图 + 人格名 + 稀有度徽章）→ 白底正文（地狱判决·首句金句）→ 二维码（带 `?src=qr` 统计）+「扫码测测你是什么东西」+ 品牌水印
- 结果页按钮/文案：`生成分享长图` → `生成分享卡片`；截图目标从 `#result-card` 改为 `#share-card`；`renderShareCard()` 每次点击时填充内容（含二维码），`waitIconLoaded()` 等图标加载完再截
- 预览遮罩 flex 居中，微信内长按保存引导不变

**踩坑记录（html2canvas 1.4.1，均已规避）**
| 坑 | 现象 | 规避 |
| --- | --- | --- |
| `box-shadow` 渲染 bug | 阴影被画成覆盖整个元素表面的灰影（如卡片整体 #D9DBDE、徽章文字被灰影盖住） | 卡片与徽章一律不用 box-shadow |
| PNG dataURL `<img>` 渲染 bug | 图片被渲染成绿色块 | 二维码直接用 canvas 承载（canvas 渲染正常），不转 PNG |
| 固定高度 + 流式内容高度计算偏差 | 内容高度在 html2canvas 与浏览器不一致，导致兄弟元素错位/裁剪 | hero 与 body 固定高度；内容全部绝对定位（html2canvas 对 absolute 支持最稳） |
| 内容溢出被 `overflow:hidden` 裁剪 | body 内容超出卡片高度时 footer 整体消失 | 内容高度预算 < 卡片高度，hero/body 固定尺寸 |

**验证**
- `build/test_share_card.py`（新增，10 项）：快测/完整两场景，PNG 生成、尺寸 1500×2000（750×1000 @2x）、落盘目检、console 零错误
- `build/test_wechat_save.py`（16 项）回归通过；像素级校验：白边纯白、二维码黑白正常、徽章金色文字渲染、布局各区域位置正确
- 源码 / deploy MD5 一致；`build/index.html` 重建（356KB）

