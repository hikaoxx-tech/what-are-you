# 传播强化 · STEPPS 优化记录

> 执行端文档 · 按 Jonah Berger《疯传》STEPPS 框架逐项审计并工程化植入
> 记录 2026-08 传播优化轮：目标是把"分享"从内容自带的偶然，变成设计出来的必然

## 一、STEPPS 评分（每次改动后重评）

| 驱动 | 改动前 | 改动后 | 本轮动作 |
| --- | --- | --- | --- |
| S 社交货币 Social Currency | 1.5 | 2 | 新增稀有度徽章（SSR/SR/R/N） |
| T 触发 Triggers | 0 | 2 | 16 人格各配 1 条日常触发线，入结果页 + 长图 |
| E 情绪 Emotion | 2 | 2 | 加载页文案随机化（5 条轮换，+1 档幽默） |
| P 公开 Public | 1 | 2 | 长图底部品牌水印（行为残留） |
| P 实用价值 Practical Value | 0 | 2 | "这张档案适合发给"面板 + 一键转发 |
| S 故事 Stories | 2 | 2 | 档案叙事保持（Trojan Horse：故事离不开品牌名） |
| **合计（原始分）** | **6.5** | **12** | 映射 10 分制 ≈ **9-10/10** |

> 判定依据：三项以上驱动达到强度 2，且 Public / Social Currency（自传播型）齐备；
> 余下 1 分留给真实世界验证——发出去测转发率，而不是纸上谈兵。

## 二、本轮具体改动

### T 触发（最大的短板，优先补）

- `js/data.js`：16 种人格各新增 `trigger` 字段，绑定**高频日常场景**：
  - ESTJ"下次开会有人问'谁还有意见'的时候"、ESTP"下次看到'仅剩最后 1 单'的时候"、
  - ENTP"下次话到嘴边准备抬杠的时候"、ISFJ"下次脱口而出'没事的'的时候"…（全部 16 条）
- 结果页新增「05 日常触发」模块：`下次×××，想想自己是哪种东西`
- 触发线在**分享长图内**（模块在 result-card 内，html2canvas 一并截取）：
  长图被存在手机相册里，之后每次翻到都是一次提醒

### P 实用价值

- 16 种人格各新增 `giveTo` 字段（转发对象描述）：
  "那个把全家行程都排进 Excel 的人"、"那个群里抬杠永远第一名的人"…
- 结果页新增「这张档案适合发给」面板 + 「复制并发给 TA」按钮
- 转发文案模板：`这份「代号·动物」的档案，我觉得写的就是你：{链接}`
- 原理：窄受众 = 更广传播（Berger：内容越精准指向某个人，越容易被转给那个人）

### S 社交货币

- 结果页 hero 新增**稀有度徽章**（金色胶囊，仅"我的结果"显示）：
  - 按 4 维得分极端度计算：满 7 分 = `SSR · 稀有物种`；6 = `SR · 稀缺物种`；5 = `R · 常见物种`；其余 = `N · 满大街都是`
  - 炫耀点诚实生成：基于自己的真实答题，不做假
- 与已有的「本机已归档 N 份」真实计数、转发挑战面板构成身份展示组合

### P 公开（行为残留）

- 分享长图底部新增品牌水印：`由《你是什么东西》非官方生成 · 扫码测测你是什么东西`
- 长图 = 可观察、可模仿、可传播的残留物：别人看到长图 → 知道从哪来 → 扫码来测

### E 情绪（保持 2 分，微调）

- 加载页文案从固定 1 条改为 5 条随机：「正在调取地狱档案库…」「正在翻你的旧账…」
  「正在给你的物种做鉴定…」「正在通知你的动物来认领你…」等

## 三、改动文件清单

| 文件 | 改动 |
| --- | --- |
| `js/data.js` | 16 人格各 +`trigger`、`giveTo`（共 32 字段） |
| `index.html` | 结果页 +稀有度徽章位、+「05 日常触发」模块、+「发给 TA」面板、+长图水印；加载页文案节点加 id |
| `js/app.js` | +`rarityOf()` 稀有度计算、渲染 trigger/giveTo、+`btn-give` 复制（复制逻辑抽成 `copyToClipboard`）、加载页随机文案 |
| `css/style.css` | +`.rarity-badge`、`.give-panel`、`.give-text`、`.card-powered` |
| `deploy/` | 与源码同步（MD5 一致） |
| `build/index.html` | 单文件构建重跑，内联全部新改动 |
| `00-文档索引.md` / `08-执行端-UI风格规范.md` | 同步本记录与新 UI 组件 |

## 四、验证方式

1. 答完题 → 结果页出现稀有度徽章（答得越极端越稀有）
2. 结果页出现「日常触发」与「发给 TA」面板，复制按钮可复制带链接文案
3. 生成分享长图 → 长图底部有品牌水印 + 日常触发模块
4. 连续多次测试 → 加载页文案随机变化
5. `js/data.js` 与 `deploy/js/data.js` MD5 一致；`build/index.html` 为最新构建
